# Render 部署详细教程（免费云平台）

## 📋 准备工作

### 必备账号
- [ ] GitHub账号（用于托管代码）
- [ ] Render账号（免费注册）

### 时间预估
- 注册账号：5分钟
- 部署网站：3分钟
- 总计：约8分钟

---

## 🚀 部署步骤

### 步骤1：准备代码（2分钟）

#### 1.1 进入项目目录
```bash
cd /workspace/projects/hotel-smart-website
```

#### 1.2 运行部署脚本
```bash
./deploy-render.sh
```

这个脚本会自动：
- 检查Git环境
- 创建render.yaml配置文件
- 初始化Git仓库

---

### 步骤2：推送到GitHub（3分钟）

#### 2.1 在GitHub创建新仓库
1. 访问：https://github.com/new
2. 仓库名：`hotel-smart-website`
3. 选择：Public（公开）
4. 勾选：Add a README file
5. 点击：Create repository

#### 2.2 配置Git并推送
```bash
# 配置Git用户（如果还没配置）
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 添加远程仓库
git remote add origin https://github.com/your-username/hotel-smart-website.git

# 推送代码
git branch -M main
git push -u origin main
```

**注意**：将 `your-username` 替换为你的GitHub用户名

---

### 步骤3：在Render创建Web Service（3分钟）

#### 3.1 注册Render账号
1. 访问：https://render.com
2. 点击右上角 "Sign Up"
3. 使用GitHub账号登录（推荐）
4. 填写基本信息，完成注册

#### 3.2 创建Web Service
1. 登录后，点击右上角 "New"
2. 选择 "Web Service"
3. 点击 "Connect" 连接GitHub
4. 授权Render访问你的GitHub账号
5. 在列表中找到 `hotel-smart-website` 仓库
6. 点击 "Connect"

---

### 步骤4：配置Web Service（2分钟）

#### 4.1 基本配置
- **Name**: `hotel-smart-website`（自动填充）
- **Region**: Singapore（推荐亚洲）或 Oregon（美洲）
- **Branch**: `main`（默认）

#### 4.2 环境配置
- **Environment**: `Python`
- **Build Command**: 自动填充（从render.yaml读取）
- **Start Command**: 自动填充（从render.yaml读取）

#### 4.3 实例类型
- **Instance Type**: `Free`
- **免费额度**：
  - 750小时/月
  - 512MB RAM
  - 0.1 CPU

#### 4.4 添加环境变量（重要）
在 "Advanced" → "Environment Variables" 点击 "Add" 添加：

```
Key: FLASK_ENV
Value: production

Key: COZE_API_KEY
Value: 你的Coze API密钥（可选）

Key: COZE_BOT_ID
Value: 你的Coze Bot ID（可选）
```

#### 4.5 点击 "Create Web Service"
- 等待约2-3分钟
- Render会自动构建和部署

---

### 步骤5：访问网站（1分钟）

#### 5.1 获取访问地址
部署完成后，Render会显示：
```
🎉 Your service is live!

https://hotel-smart-website.onrender.com
```

#### 5.2 访问测试
点击链接或直接在浏览器访问：
```
https://hotel-smart-website.onrender.com
```

#### 5.3 测试功能
- ✅ 首页正常显示
- ✅ 咨询页面可访问
- ✅ 方案生成页面可访问
- ✅ API接口正常

---

## 🔧 配置说明

### render.yaml 文件解析

项目已自动创建 `render.yaml` 配置文件：

```yaml
services:
  - type: web
    name: hotel-smart-website
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:5000 app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: COZE_API_KEY
        sync: false
      - key: COZE_BOT_ID
        sync: false
```

**参数说明**：
- `type: web` - Web服务类型
- `env: python` - Python环境
- `plan: free` - 免费计划
- `buildCommand` - 构建命令（安装依赖）
- `startCommand` - 启动命令
- `envVars` - 环境变量
  - `sync: false` - 不同步（手动配置）

---

## 📊 部署过程监控

### 查看部署日志
在Render控制台：
1. 进入你的Web Service
2. 点击 "Events" 标签
3. 查看部署进度和日志

### 常见状态
- **Building** - 正在构建
- **Deploying** - 正在部署
- **Live** - 部署成功
- **Failed** - 部署失败（查看日志排查）

---

## 🎯 自定义域名（可选）

### 步骤1：购买域名
- 阿里云：https://wanwang.aliyun.com
- 腾讯云：https://dnspod.cloud.tencent.com
- Namecheap：https://www.namecheap.com

### 步骤2：在Render配置域名
1. 进入Web Service → "Settings"
2. 点击 "Domains"
3. 输入你的域名（如 `hotel-smart.com`）
4. 点击 "Add Domain"

### 步骤3：配置DNS解析
Render会显示需要添加的DNS记录：

```
类型: CNAME
名称: @
值: hotel-smart-website.onrender.com
```

在域名服务商处添加该记录。

### 步骤4：启用HTTPS
Render会自动为自定义域名配置SSL证书。

---

## 🔐 安全配置

### 环境变量保护
- ✅ 不要在代码中硬编码密钥
- ✅ 使用Render的环境变量功能
- ✅ 不要将.env文件推送到GitHub

### API密钥配置
如果需要调用Coze API，在Render添加：

```
Key: COZE_API_KEY
Value: pat_xxxxxxxxxxxxxxxx
```

```
Key: COZE_BOT_ID
Value: 7389xxxxxxxxxxxxxx
```

---

## 📝 部署后检查清单

### 功能检查
- [ ] 首页可访问
- [ ] 咨询页面可访问
- [ ] 方案生成页面可访问
- [ ] API接口正常响应
- [ ] 静态资源加载正常

### 性能检查
- [ ] 页面加载速度正常
- [ ] 无明显卡顿
- [ ] 响应式设计正常

### 安全检查
- [ ] 环境变量已配置
- [ ] API密钥未泄露
- [ ] HTTPS已启用

---

## 🐛 常见问题排查

### 问题1：部署失败
**原因**：依赖安装失败或启动命令错误

**解决**：
1. 查看 "Events" 日志
2. 检查 `requirements.txt` 是否正确
3. 检查 `startCommand` 是否正确

### 问题2：访问404
**原因**：路由配置错误

**解决**：
1. 检查 `app.py` 中的路由配置
2. 确保使用 `/` 而不是 `/index.html`

### 问题3：静态资源加载失败
**原因**：静态文件路径错误

**解决**：
1. 检查 `app.py` 是否配置了static文件夹
2. 确保使用 `/static/css/style.css` 绝对路径

### 问题4：环境变量未生效
**原因**：环境变量配置错误

**解决**：
1. 检查Render的环境变量配置
2. 确保Key和Value都正确填写
3. 重启Web Service

### 问题5：免费额度用完
**原因**：超过750小时/月

**解决**：
1. 升级到付费计划（约7美元/月）
2. 或暂停服务

---

## 📈 免费额度说明

### Render免费计划
- ✅ **750小时/月**（约25天）
- ✅ **512MB RAM**
- ✅ **0.1 CPU**
- ✅ **自动SSL**
- ✅ **自定义域名**

### 超出免费额度后
- 服务会自动休眠
- 需要升级到付费计划
- 或手动重启服务

### 付费计划
- **Starter**: $7/月（512MB RAM）
- **Standard**: $25/月（2GB RAM）
- **Pro**: $100/月（8GB RAM）

---

## 🔄 更新部署

### 修改代码后重新部署

#### 方式1：自动部署（推荐）
1. 修改代码
2. 提交到GitHub
3. Render自动检测并重新部署

```bash
git add .
git commit -m "Update feature"
git push
```

#### 方式2：手动部署
1. 在Render控制台
2. 点击 "Manual Deploy"
3. 选择分支
4. 点击 "Deploy"

---

## 📚 更多资源

### Render官方文档
- 官网：https://render.com/docs
- Web Services：https://render.com/docs/web-services
- 环境变量：https://render.com/docs/environment-variables

### GitHub官方文档
- Git基础：https://docs.github.com/en/get-started/git-and-github/learning-git
- 推送代码：https://docs.github.com/en/get-started/using-git/pushing-commits-to-a-remote-repository

---

## ✅ 完成检查

部署完成后，确认以下项目：

- [ ] 网站可正常访问
- [ ] 所有页面功能正常
- [ ] API接口响应正常
- [ ] 环境变量已配置（如需要）
- [ ] 自定义域名已配置（如需要）
- [ ] HTTPS已启用
- [ ] 部署日志无错误

---

## 🎉 部署成功！

**访问地址**：https://hotel-smart-website.onrender.com

**管理地址**：https://dashboard.render.com

---

## 💡 下一步

1. **配置Coze API**（如需真实Skill调用）
   - 在Render添加环境变量
   - COZE_API_KEY
   - COZE_BOT_ID

2. **自定义域名**（可选）
   - 购买域名
   - 配置DNS解析

3. **监控服务**
   - 查看访问日志
   - 监控性能

---

**恭喜您的网站已成功部署到Render！** 🎊

如有问题，请查看Render控制台的 "Events" 日志。
